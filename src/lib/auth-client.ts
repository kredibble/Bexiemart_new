/**
 * Better Auth Client Configuration
 *
 * Re-exported from centralized config for backward compatibility.
 * All new code should import from '@/config' instead.
 */
export { authClient, authConfig, authKeys } from '@/config/auth';
